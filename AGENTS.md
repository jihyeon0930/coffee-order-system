# 프로젝트 개발 규칙

이 저장소에서 수행하는 모든 개발 작업은 아래 규칙을 반드시 따른다. 새로운 채팅에서도 이 파일을 가장 먼저 확인하고 동일한 프로젝트 규칙과 작업 경로를 유지한다.

## 프로젝트 작업 위치

### Working Directory

1. 항상 이 프로젝트의 루트 디렉터리를 기준으로 작업한다.
2. 작업을 시작하기 전에 반드시 `pwd`로 현재 작업 디렉터리를 확인한다.
3. 현재 위치가 프로젝트 루트가 아니라면 임의의 다른 폴더에서 작업하지 말고 프로젝트 루트로 이동한 뒤 작업한다.
4. 새로운 프로젝트를 생성하거나 다른 위치에 동일한 프로젝트를 복사하지 않는다.
5. 사용자가 명시적으로 요청하지 않는 한 프로젝트 루트 밖의 파일은 수정하지 않는다.

## 개발 단위

1. GitHub Issue 하나당 하나의 기능만 구현한다.
2. Issue 내용이 확인되지 않거나 구현 범위가 불명확하면 임의로 구현하지 말고 먼저 확인한다.

## 작업 시작 절차

모든 새로운 작업 또는 새로운 채팅에서는 다음 순서를 반드시 수행한다.

1. `AGENTS.md`를 먼저 읽는다.
2. `pwd`로 현재 작업 디렉터리를 확인한다.
3. Git 상태를 확인한다.
4. 현재 브랜치를 확인한다.
5. GitHub Issue 내용을 확인한다.
6. 구현 범위와 이번 작업에서 만들 결과물을 한국어로 요약한다.
7. 구현을 시작한다.

## 작업 중 금지사항

다음 행동은 사용자의 명시적인 요청 없이 수행하지 않는다.

- 프로젝트를 다른 경로에 새로 생성하거나 기존 프로젝트를 복사하는 행위
- 프로젝트 루트를 변경하는 행위
- Issue 범위를 벗어난 불필요한 리팩토링
- 전체 패키지 구조 변경
- 대규모 파일 이동
- 기존 `study/` 폴더 또는 그 안의 파일 삭제 및 덮어쓰기
- 기존 `docs/` 삭제
- 강제 Push
- Force Reset
- Force Checkout

## 학습 노트

1. 구현이 끝나면 프로젝트 루트의 `study/` 폴더에 학습 노트를 반드시 작성한다.
2. 파일명은 Issue 번호에 맞춰 `study/01-xxx.md`, `study/02-xxx.md` 형식을 사용한다.
3. 같은 Issue의 기존 학습 노트가 있다면 절대로 덮어쓰지 말고 내용을 확인한 뒤 사용자에게 알린다.
4. 학습 노트는 단순한 코드 변경 요약이 아니라 백엔드 개발자의 학습 기록으로 작성하며, 필요하면 코드 예시를 포함한다.
5. 학습 노트는 다음 형식을 따른다.

```markdown
# 이번 이슈에서 구현한 기능

## 왜 필요한 기능인가

## 사용한 기술

## 핵심 코드 설명

## 오늘 새롭게 배운 내용

## 어려웠던 점

## 어떻게 해결했는가

## 다음에 공부할 내용

## 참고 자료
```

## 작업 종료 절차

구현이 끝나면 반드시 다음 순서를 수행한다.

1. 프로젝트에 맞는 Build를 수행한다.
2. 프로젝트에 맞는 Test를 수행한다.
3. 해당 Issue의 `study/` 문서를 작성한다.
4. 변경 내용에 따라 `README`와 `docs` 수정이 필요한지 확인하고, 필요하면 함께 업데이트한다.
5. 변경사항과 Build/Test 결과를 한국어로 요약한다. 검증이 실패하면 원인과 미해결 사항을 명확히 보고한다.
6. Conventional Commits 형식으로 Commit한다.
7. Push 전에 변경사항을 한국어로 공유하고, 사용자 승인 이후에만 Push한다.

## Git 규칙

1. 커밋 메시지는 Conventional Commits 형식을 사용한다.
2. 예: `feat: 메뉴 등록 API 구현`
3. 강제 Push, Force Reset, Force Checkout은 사용자가 명시적으로 요청하지 않는 한 수행하지 않는다.

## 커뮤니케이션

Issue 확인, 범위 요약, 구현 계획, 진행 상황, 검증 결과, 변경사항 요약을 포함한 모든 설명은 한국어로 작성한다.
